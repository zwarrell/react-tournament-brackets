export declare function calculateSVGDimensions(numOfRows: any, numOfColumns: any, rowHeight: any, columnWidth: any, canvasPadding: any, roundHeader: any, currentRound?: string): {
    gameWidth: number;
    gameHeight: any;
    startPosition: number[];
};
